// Task1 for this lesson https://javascript.info/ifelse#if-a-string-with-zero

// will alert be shown ? Yes, as "0" is converted to the boolean value (true) 
// as the string isnot empty
if ("0") {
  alert( 'Hello' );
}